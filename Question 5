1 Rust’s closures are anonymous functions you can save in a variable 
2 It can also be pass as arguments to other functions. 
3 Programmer can create the closure in one place and then call the closure to evaluate it in a different context. 
4 Unlike functions, closures can capture values from the scope in which they’re defined.
